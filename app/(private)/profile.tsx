import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Profile = () => {
  return (
    <>
      <SafeAreaView>
        <Text>There is a private profile page.</Text>
      </SafeAreaView>
    </>
  );
};

export default Profile;
